// Shared game state module
const GameState = {
    currentLevel: 1,
    maxLevel: 5,
    playerHealth: 100,
    maxHealth: 100,
    ghosts: [], // Array to store ghost entities and their health
    gameOver: false // Flag to track if game is over
};

